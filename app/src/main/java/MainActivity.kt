package com.example.simpletextmessage

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import android.widget.TextView
import res.layout.*

/**class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(res.xml.backup_rules)

        // Control the greeting string here
        val greetingMessage = "Hello, welcome to the app!"

        // Find the TextView by its ID and set the greeting message
        val messageTextView: TextView = findViewById(R.id.messageTextView)
        messageTextView.text = greetingMessage
    }
}**/